name: Build and Release

on:
  push:
    branches:
      - master  # Workflow запускается при пуше в ветку master

jobs:
  build:
    runs-on: windows-latest  # Используем Windows для сборки

    steps:
      # Шаг 1: Проверка кода из репозитория
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 50  # Загружаем последние 50 коммитов

      # Шаг 2: Получение информации о пуше
      - name: Get push details
        id: push-details
        shell: pwsh
        run: |
          # Получаем хэш последнего коммита
          $commitHash = git rev-parse HEAD
          Write-Host "✅ Commit hash: $commitHash"

          # Получаем описание пуша (сообщение коммита)
          $commitMessage = git log -1 --pretty=%B
          Write-Host "✅ Commit message: $commitMessage"

          # Экранируем спецсимволы в сообщении коммита
          $escapedCommitMessage = $commitMessage -replace '"', '`"' -replace "'", "''" -replace "`n", " " -replace "`r", " "
          Write-Host "✅ Escaped commit message: $escapedCommitMessage"

          # Получаем список измененных файлов
          $changedFiles = git diff --name-only HEAD^ HEAD
          Write-Host "✅ Changed files:"
          $changedFiles | ForEach-Object { Write-Host "  $_" }

          # Сохраняем данные в выходные переменные
          echo "commit_hash=$commitHash" >> $env:GITHUB_OUTPUT
          echo "commit_message=$escapedCommitMessage" >> $env:GITHUB_OUTPUT
          echo "changed_files=$($changedFiles -join ';')" >> $env:GITHUB_OUTPUT

      # Шаг 3: Установка Python 2.7
      - name: Set up Python 2.7
        uses: LizardByte/setup-python-action@master
        with:
          python-version: '2.7'  # Явно указываем версию Python
        id: setup-python

      # Шаг 4: Проверка сообщения коммита на ключевые слова
      - name: Check commit message for release keywords
        id: check_commit
        shell: pwsh
        run: |
          # Получаем сообщение коммита
          $commitMessage = "${{ steps.push-details.outputs.commit_message }}"
          Write-Host "🔍 Checking commit message for release keywords..."

          # Получаем список измененных файлов
          $changedFiles = "${{ steps.push-details.outputs.changed_files }}" -split ";"
          Write-Host "✅ Changed files: $changedFiles"

          # Определяем шаблоны для поиска ключевых слов
          $releasePattern = "(?i)release|релиз"
          $allPattern = "(?i)all|всех|все|всё"

          # Проверяем, содержит ли сообщение коммита ключевые слова
          $isRelease = $commitMessage -match $releasePattern
          $isAll = $commitMessage -match $allPattern

          # Проверяем, изменен ли файл .github/game_version.json
          $isGameVersionChanged = $changedFiles -contains ".github/game_version.json"

          # Логируем результаты проверки
          if ($isRelease) {
              Write-Host "✅ Commit message contains release keyword."
              Write-Host "✅ commit_message=$commitMessage"
              Write-Host "✅ is_release=true"
              echo "commit_message=$commitMessage" >> $env:GITHUB_OUTPUT
              echo "is_release=true" >> $env:GITHUB_OUTPUT

              if ($isAll -or $isGameVersionChanged) {
                  Write-Host "✅ is_all=true"
                  echo "is_all=true" >> $env:GITHUB_OUTPUT
              } else {
                  Write-Host "✅ is_all=false"
                  echo "is_all=false" >> $env:GITHUB_OUTPUT
              }
          } else {
              Write-Host "❌ Commit message does not contain release keyword. Skipping build."
              Write-Host "❌ is_release=false"
              echo "is_release=false" >> $env:GITHUB_OUTPUT
              Write-Host "❌ is_all=false"
              echo "is_all=false" >> $env:GITHUB_OUTPUT
          }

      # Шаг 5: Дебаг и тестирование
      - name: Debug and test
        id: debug-test
        shell: pwsh
        run: |
          # Выводим данные из предыдущих шагов
          Write-Host "🚀 Debug information:"
          Write-Host "Commit hash: ${{ steps.push-details.outputs.commit_hash }}"
          Write-Host "Commit message: ${{ steps.push-details.outputs.commit_message }}"
          Write-Host "Changed files: ${{ steps.push-details.outputs.changed_files }}"
          Write-Host "Is release: ${{ steps.check_commit.outputs.is_release }}"
          Write-Host "Is all: ${{ steps.check_commit.outputs.is_all }}"

          # Пример использования переменных в PowerShell
          $commitMessage = "${{ steps.push-details.outputs.commit_message }}"
          Write-Host "✅ Escaped commit message in PowerShell: $commitMessage"

          # Проверка кодировки консоли и Python
          Write-Host "🔍 Checking console encoding for Python..."
          & "python" -c "import sys; print('Python default encoding: ' + sys.getdefaultencoding())"
          & "python" -c "import sys; print('Python stdout encoding: ' + (sys.stdout.encoding or 'None'))"
          & "python" -c "import sys; print('Python file system encoding: ' + sys.getfilesystemencoding())"

          # Устанавливаем кодировку UTF-8 для Python
          $env:PYTHONIOENCODING = "utf-8"
          Write-Host "✅ Set PYTHONIOENCODING to utf-8."

          # Выводим сообщение с использованием UTF-8
          Write-Host "🔍 Testing Python output with special characters..."
          & "python" -c "import sys; print('✅ Escaped commit message in Python: ' + sys.argv[1].encode('utf-8').decode('utf-8'))" "$commitMessage"

          # Тестируем вывод с различными спецсимволами
          # Исправляем строку с тестовым сообщением, экранируя двойные кавычки и обратные слэши
          $testMessage = "Test message with special characters: \`nNew line, \`tTab, 'Single quotes', \`"Double quotes\`", русский текст"
          Write-Host "🔍 Testing Python output with special characters: $testMessage"
          & "python" -c "import sys; print('✅ Test message in Python: ' + sys.argv[1].encode('utf-8').decode('utf-8'))" "$testMessage"

      # Шаг 6: Углубленный дебаг и тестирование переменных
      - name: Deep debug and test variables
        id: deep-debug
        shell: pwsh
        run: |
          # Выводим все переменные из предыдущих шагов
          Write-Host "🚀 Deep debug information:"
          Write-Host "Commit hash: ${{ steps.push-details.outputs.commit_hash }}"
          Write-Host "Commit message: ${{ steps.push-details.outputs.commit_message }}"
          Write-Host "Changed files: ${{ steps.push-details.outputs.changed_files }}"
          Write-Host "Is release: ${{ steps.check_commit.outputs.is_release }}"
          Write-Host "Is all: ${{ steps.check_commit.outputs.is_all }}"

          # Проверяем, что переменные корректно передаются
          if (-not "${{ steps.push-details.outputs.commit_hash }}") {
              Write-Host "❌ Commit hash is empty!"
              exit 1
          }
          if (-not "${{ steps.push-details.outputs.commit_message }}") {
              Write-Host "❌ Commit message is empty!"
              exit 1
          }
          if (-not "${{ steps.push-details.outputs.changed_files }}") {
              Write-Host "❌ Changed files list is empty!"
              exit 1
          }
          if (-not "${{ steps.check_commit.outputs.is_release }}") {
              Write-Host "❌ Is release flag is empty!"
              exit 1
          }
          if (-not "${{ steps.check_commit.outputs.is_all }}") {
              Write-Host "❌ Is all flag is empty!"
              exit 1
          }

          Write-Host "✅ All variables are correctly set and passed."

      # Шаг 7: Проверка корректности передачи текста с учетом спецсимволов и русского языка
      - name: Check text handling with special characters and Russian language
        id: text-check
        shell: pwsh
        run: |
          # Получаем сообщение коммита из предыдущих шагов
          $commitMessage = "${{ steps.push-details.outputs.commit_message }}"
          Write-Host "🔍 Checking text handling with special characters and Russian language..."

          # Проверяем корректность передачи текста с учетом спецсимволов и русского языка
          # Исправляем строку с тестовым сообщением, экранируя двойные кавычки и обратные слэши
          $testMessage = "Test message with special characters: \`nNew line, \`tTab, 'Single quotes', \`"Double quotes\`", русский текст"
          Write-Host "🔍 Test message: $testMessage"

          # Проверяем корректность передачи текста в Python
          Write-Host "🔍 Testing Python output with special characters and Russian language..."
          & "python" -c "import sys; print('✅ Test message in Python: ' + sys.argv[1].encode('utf-8').decode('utf-8'))" "$testMessage"

          # Проверяем корректность передачи текста в PowerShell
          Write-Host "🔍 Testing PowerShell output with special characters and Russian language..."
          Write-Host "✅ Test message in PowerShell: $testMessage"

          # Проверяем корректность передачи текста с учетом спецсимволов и русского языка в переменных
          Write-Host "🔍 Testing variable handling with special characters and Russian language..."
          Write-Host "✅ Commit message in PowerShell: $commitMessage"
          & "python" -c "import sys; print('✅ Commit message in Python: ' + sys.argv[1].encode('utf-8').decode('utf-8'))" "$commitMessage"

      # Шаг 8: Проверка корректности передачи переменных между шагами
      - name: Check variable passing between steps
        id: variable-check
        shell: pwsh
        run: |
          # Получаем все переменные из предыдущих шагов
          $commitHash = "${{ steps.push-details.outputs.commit_hash }}"
          $commitMessage = "${{ steps.push-details.outputs.commit_message }}"
          $changedFiles = "${{ steps.push-details.outputs.changed_files }}"
          $isRelease = "${{ steps.check_commit.outputs.is_release }}"
          $isAll = "${{ steps.check_commit.outputs.is_all }}"

          Write-Host "🔍 Checking variable passing between steps..."

          # Проверяем корректность передачи переменных
          Write-Host "✅ Commit hash: $commitHash"
          Write-Host "✅ Commit message: $commitMessage"
          Write-Host "✅ Changed files: $changedFiles"
          Write-Host "✅ Is release: $isRelease"
          Write-Host "✅ Is all: $isAll"

          # Проверяем, что переменные не пустые
          if (-not $commitHash) {
              Write-Host "❌ Commit hash is empty!"
              exit 1
          }
          if (-not $commitMessage) {
              Write-Host "❌ Commit message is empty!"
              exit 1
          }
          if (-not $changedFiles) {
              Write-Host "❌ Changed files list is empty!"
              exit 1
          }
          if (-not $isRelease) {
              Write-Host "❌ Is release flag is empty!"
              exit 1
          }
          if (-not $isAll) {
              Write-Host "❌ Is all flag is empty!"
              exit 1
          }

          Write-Host "✅ All variables are correctly passed between steps."

      # Шаг 9: Проверка корректности работы с файлами
      - name: Check file handling with special characters and Russian language
        id: file-check
        shell: pwsh
        run: |
          # Получаем список измененных файлов из предыдущих шагов
          $changedFiles = "${{ steps.push-details.outputs.changed_files }}" -split ";"
          Write-Host "🔍 Checking file handling with special characters and Russian language..."

          # Проверяем корректность работы с файлами
          foreach ($file in $changedFiles) {
              Write-Host "🔍 Checking file: $file"
              if (Test-Path $file) {
                  Write-Host "✅ File exists: $file"
                  $content = Get-Content $file -Raw
                  Write-Host "✅ File content: $content"
              } else {
                  Write-Host "❌ File does not exist: $file"
                  exit 1
              }
          }

          Write-Host "✅ All files are correctly handled."